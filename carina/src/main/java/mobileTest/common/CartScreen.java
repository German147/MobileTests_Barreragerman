package mobileTest.common;

import com.qaprosoft.carina.core.gui.AbstractPage;
import org.openqa.selenium.WebDriver;

public abstract class CartScreen extends AbstractPage {
    public CartScreen(WebDriver driver) {
        super(driver);
    }
}
